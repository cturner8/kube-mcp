apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-mcp.fullname" . }}-test-connection"
  labels:
    {{- include "kube-mcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kube-mcp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
