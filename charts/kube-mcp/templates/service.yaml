apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-mcp.fullname" . }}
  labels:
    {{- include "kube-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "kube-mcp.selectorLabels" . | nindent 4 }}
