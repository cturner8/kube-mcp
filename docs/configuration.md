# Configuration Options

The MCP server can be customised via CLI flags or environment variables.

When using the chart based deployment, contents of the `mcp` block of the supplied values determines the value of the relevant environment variable.

// TODO: insert table with configuration, columns for CLI flag, env variable, chart value and description